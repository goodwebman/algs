## Задача

Проверить, читается ли строка одинаково слева направо и справа налево. Регистр не важен, всё, что не буква и не цифра, игнорируется.

`"A man, a plan, a canal: Panama"` — палиндром. `"race a car"` — нет.

## Наивное решение

```js
const isPalindrome = (s) => {
  const clean = s.toLowerCase().replace(/[^a-z0-9]/g, '');
  return clean === [...clean].reverse().join('');
};
```

Работает, читается, и в проде я бы такое принял. Но здесь есть что улучшить: `reverse().join('')` строит вторую копию строки целиком, и сравнение всегда идёт до конца — даже когда первая же пара символов не совпала.

<Note tone="trap" title="И одна настоящая ошибка">
  Регулярка `[^a-z0-9]` выкидывает всё, кроме латиницы. На кириллице она сотрёт строку целиком, а
  `[^a-zа-я0-9]` — почти целиком: буква «ё» **не входит** в диапазон `а-я` в Unicode (она стоит после
  «я», в позиции U+0451). Правильно — `\p{L}` с флагом `u`.
</Note>

## Инсайт: чистим один раз, сравниваем навстречу

Одна регулярка выбрасывает всё лишнее, дальше работают два указателя: пока `clean[left] === clean[right]` — сдвигаем оба внутрь, первое расхождение сразу даёт `false`. Вторая копия строки не строится, и на `"race a car"` цикл обрывается на первой же паре вместо полного прохода.

Строка `".,!"` после очистки становится пустой: `right` равен −1, условие `left < right` ложно с самого начала — ответ `true`, без единого сравнения.

<Note tone="info" title="Есть и вариант без очистки">
  Мусор можно пропускать прямо в цикле, тогда дополнительной памяти не нужно совсем — O(1) вместо
  O(n). Ценой того, что внутри появляются два `continue` с проверкой `left < right`, иначе на строке
  из одних знаков препинания указатель уедет за границу.
</Note>

## Как это выглядит по шагам

<Viz algo="valid-palindrome" />

## Сложность

**Время — O(n).** Каждый указатель проходит свою половину строки, суммарно каждый символ смотрится один раз.

**Память — O(n)** на очищенную копию строки. Это плата за читаемость: вариант с пропуском мусора на лету обходится O(1).

## JS-специфика: строка — это не массив символов

`"👍".length === 2`. Строки в JS — это последовательности UTF-16 code units, а не символов. Эмодзи, редкие иероглифы и некоторые математические символы занимают по две ячейки (суррогатная пара), и обращение `s[i]` вернёт половину символа.

```js
const s = '👍a👍';
s.length;        // 5, а не 3
s[0];            // '\uD83D' — половинка эмодзи
[...s].length;   // 3 — спред идёт по code points
```

Поэтому в разборе очищенная строка сразу раскладывается спредом — `[...input.toLowerCase().replace(…)]`: спред-оператор и `for…of` обходят строку по code points, а индексирование — нет. Для задач с эмодзи это разница между работающим кодом и мусором.

Отдельно: `\p{L}` (любая буква) и `\p{N}` (любая цифра) требуют флага `u` — без него движок не поймёт синтаксис Unicode property escapes и бросит `SyntaxError`.

## Ловушки

- Строка из одних знаков препинания и пустая строка — палиндромы. Многие решения на них падают.
- `toLowerCase()` на каждой итерации создаёт новую строку. Для двух символов это копейки, но в горячем цикле над длинными строками — заметно.
- Турецкая «I»: `'I'.toLowerCase()` даёт `'i'`, но в турецкой локали правильный ответ — `'ı'`. Если задача про локали, нужен `toLocaleLowerCase`.

## Похожие задачи

- [Пара с заданной суммой](/t/two-pointers/two-sum-sorted) — та же схема навстречу, но по сумме.
- [Три числа с нулевой суммой](/t/two-pointers/three-sum) — указатели внутри перебора.

## Решай сам

<Task slug="valid-palindrome" />

## Как это было решено изначально

<OriginalSource file="two-pointers/is-palindrom.js" note="Регулярка `[^a-z0-9а-я]` вырезает «ё»: в Unicode она стоит после «я» и в диапазон не входит." />
